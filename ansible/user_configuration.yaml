---
- name:
  hosts: test
  become: yes

  tasks:
    - name: Create user
      ansible.builtin.user:
        name: tempuser
        shell: /bin/bash
        password: "{{ temppass | password_hash('sha512') }}"


